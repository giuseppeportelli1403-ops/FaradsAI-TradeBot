# Project Status — Auto-Updated

> ## 🔔 FIRST THING TO SURFACE NEXT SESSION (Giuseppe asked 2026-04-24) 🔔
>
> Two items he explicitly asked to be reminded about:
>
> 1. **Node 22 upgrade on VPS** — still pending, scheduled for demo-end (~2026-05-04). `yahoo-finance2@3.14.0` warns about requiring Node ≥22 while VPS runs 20.20.2. Library works with just a warning today. Tracked in `~/.claude/projects/C--Program-Files-Git/memory/project_farad_demo_end_todo.md` item #3.
>
> 2. **ICT Agent `log_trade` bug** — surfaced in VPS pm2-err.log on 2026-04-24: `[ICT Agent] Tool log_trade failed: insertTrade: required field(s) missing: id`. Pre-existing (NOT introduced by today's work). Check `src/mcp-server/tools/trading-tools.ts` `log_trade` definition vs `src/database/index.ts` `insertTrade` — probably a missing field in the tool's zod schema, or the agent thinks `id` is autogenerated but the DB requires one. Tracked in memory item #9.

Last updated: 2026-04-24 ~09:45 UTC (day 5 of demo) — VIX/DXY removed + Alpha Vantage → MarketAux swapped
Project: BetterOpsAI Trading Bot ("Farad")
Branch: **master**
Last commit on master: `919e307` — "refactor: swap news provider from Alpha Vantage to MarketAux"
VPS head: `919e307` (synced — pm2 restart #53 clean, preflight 0 errors)

## 🚢 What shipped today — two atomic refactors

Both deployed to VPS; bot online, preflight clean, zero regressions in 246 local tests.

| Commit | Purpose | Files changed |
|---|---|---|
| `b2a966b` | **VIX/DXY removal.** Drop `fetchVix`/`fetchDxy` and their MCP tools from the researcher path. Free-tier Twelve Data proxies for DXY were misleading (ETF tickers at 25–70 vs real ~99); VIX required $229/mo Pro. VIX/DXY remain in `TWELVE_DATA_UNAVAILABLE` defensively so stray LLM tool calls return empty. `RegimeData.vix*`/`dxy*` fields now optional so historical briefs in SQLite still parse. | 10 files |
| `919e307` | **News provider swap AV → MarketAux.** AV's 25 req/day was exhausted by 07:00 UTC daily leaving bot news-blind through NY Open. MarketAux = 100 req/day + 30/min burst (no rate-limit bucket needed). `fetchNewsContext` contract preserved end-to-end — same `NewsItem` shape, same A/B/C category thresholds, same 5 resilience layers. Symbol mapping verified live: FX pairs = bare symbols (no `=X`), commodities = ETF proxies (`GLD`/`SLV`/`USO` — same as AV had). | 10 files |

**Local tests:** 249 → 246 (net -3 from AV burst-limit test deletion and AV-specific resilience tests replaced with MarketAux equivalents).

**Supporting docs commits:** `13852c4` (plan), `6827f6b` (MarketAux API findings), `720516a` (ETF-proxy follow-up resolution).

## 🎯 Current production config

**Universe (7 — ICT only):** GOLD, SILVER, OIL_CRUDE, EURUSD, GBPUSD, USDJPY, AUDUSD

**Active cron schedule (6 entries, unchanged today):**
```
*/5 * * * *    Split-position monitor + candle detection → ICT Agent
*/8 * * * *    Capital.com session keep-alive ping
30 5 * * *     Market Researcher (daily pre-London)
0 22 * * 0     Market Researcher (weekly)
0 0 * * 0      Weekly Review Agent
5 0 * * *      Reject metrics dump (previous UTC day)
```

**Kill switches:** 6% daily (code-enforced), 10% weekly (prompt-advisory).
**Demo gate:** DEMO_RELAXED_GATES=true.

**External data sources (post-2026-04-24):**
- Twelve Data (Grow tier) — OHLC candles
- Finnhub — Economic calendar
- Yahoo Finance — Sector strength
- FRED — Yield curve
- MarketAux — News with per-entity sentiment (was Alpha Vantage)

## 📍 First MarketAux live confirmation

When you open next session, first sanity check:

```bash
ssh bot@162.55.212.198 "grep 'MarketAux news' /home/bot/trading-bot/data/pm2-out.log | tail -10"
```

Expected format: `[Market Data] MarketAux news for EURUSD (as EURUSD): N articles`. The 05:30 UTC Researcher run will produce the first volume of these (or the next ICT cycle, whichever fires first after deploy at ~09:42 UTC).

If you see `MARKETAUX_API_KEY not set` → env didn't load (.env backup on VPS at `.env.bak-<timestamp>`).
If you see `MarketAux daily rate limit reached` within an hour of deploy → bad key or quota already burned upstream. Rare.

## 🛟 Rollback, if ever needed

```bash
ssh bot@162.55.212.198 "cd /home/bot/trading-bot && git reset --hard b2a966b && npm run build && mv .env.bak-<latest> .env && pm2 restart trading-bot"
```

Keeps Part A (VIX/DXY removal, low risk), drops Part B (MarketAux swap).

## 🚧 Deferred / open items

1. **Node 22 upgrade** — demo-end. See top banner. Memory item #3.
2. **ICT Agent `log_trade` bug** — next session. See top banner. Memory item #9.
3. **Scanner throttle revert decision (`RANKING_TTL_MS` = 0 vs 5 min)** — demo-end. Memory item #1.
4. **P1 live verification (limit orders)** — still outstanding from 2026-04-23. Check for `orderType: LIMIT` + `workingOrderId` on the next kill-zone cycle. P1 shipped as `df3cfbb` + `ef9f4d5`.
5. **P2 news-softening verification** — outstanding from 2026-04-23. Look for 50%-size trades when opposing Cat-A news present. P2 shipped as `fd8afa9`.
6. **Capital.com ECONNABORTED alert tuning** — post-demo. Memory item #8.
7. **GitHub Actions `checkout@v4` / `setup-node@v4` bump** — becomes an error after 2026-09-16. Memory item #6.

## 🧘 Session close state (2026-04-24)

Refactoring session — two atomic commits shipped (`b2a966b`, `919e307`), both deployed to VPS, preflight clean, 246/246 tests green. Working directory clean. No new design decisions, no open PRs. Part A removed the free-tier Twelve Data proxy misreads from the researcher surface; Part B eliminates the NY-Open news blindness caused by AV's daily quota ceiling.

Giuseppe's explicit asks for next session: surface Node 22 and log_trade reminders at the top.
